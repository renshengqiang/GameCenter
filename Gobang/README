王小春的算法思想：
他的基本思想是对当前的静态局面进行评估...
用一个15*15的二维数组来表示一个棋盘, 每个元素有三种值, 白(1), 黑(2), 无棋子(0).
对棋盘进行扫描, 当遇到一个不是空的位置之后对它进行4个方向的分析--水平, 垂直, 左对角, 右对角. 具体是把当前的棋子在的行, 列和对角转化到一个一维的数组中. 由一个analysisLine(int TempArray[], int len , int nCurPos)来计算其中的各种棋型, 并保存到一个m_lineRecord数组之中去...再把m_lineRecord中的值转化到一个三维数组m_analysisTable[j][k], i,j是当前棋子的位置, k是方向, 有四个取值...当完成了所有的扫描工作之后是由m_typeRecord[side][棋型]来记录个中棋型的数量, 最后再对这个数组进行扫描, 对不同的棋型进行不同的给分...最后还会有个位置分数的加权, 可以对计算机刚开始的走棋形成指导...

1. 极大极小值算法是对所有的分支进行选择，最后得到想要的结果；负极大值算法是极大极小值算法的一种变体，利用了负数的性质：绝对值越大的负数越小，然后让极小值节点的值变为负数，最后统一取最大值进行选择。
2. Alpha-Beta算法是对极大极小值算法的一种改进，它的想法是若当前节点的孩子的值对当前节点的父亲节点的值没有影响，则无需继续判断当前节点后面的孩子。
测试数据如下：
3层的博弈树，博弈分支为11139520，如果没有任何的节点排序，则裁剪的分支数为3911069；如果进行一个排序，将棋盘中间的节点优先处理，则裁剪的分支数为6374738。但是效果还是很差，每一步的判断需要10秒钟左右，计算机一秒钟可以处理的分支数目约为50万个。

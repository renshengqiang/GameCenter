#include "Gobang.h"

GobangWindow::GobangWindow(int x, int y):
    mWidth(600),mHeight(400)
{
    QWidget::setGeometry(x,y,mWidth,mHeight);
}
void GobangWindow::paintEvent(QPaintEvent *event)
{
    QPainter painter(this);

}
